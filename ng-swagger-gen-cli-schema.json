{
  "$schema": "http://json-schema.org/schema",
  "id": "https://github.com/spitfjre/ng-swagger-gen-cli/blob/master/ng-swagger-gen-cli-schema.json",
  "title": "Options for ng-swagger-gen-cli",
  "type": "object",
  "required": ["configurations"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "configurations": {
      "type": "array",
      "required": ["defaultUrl", "name", "swaggerGen"],
      "properties": {
        "defaultUrl": {
          "description": "The default URL of the swagger descriptor in JSON format.",
          "type": "string"
        },
        "name": {
          "description": "The name of the configuration.",
          "type": "string"
        },
        "swaggerGen": {
          "description": "The location of the ng-swagger-gen file.",
          "type": "string"
        }
      }
    }
  }
}
